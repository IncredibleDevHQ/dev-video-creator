import React from 'react'
import { FiLoader } from 'react-icons/fi'
import { IconBaseProps } from 'react-icons/lib'

const Loader = (props: IconBaseProps) => <FiLoader {...props} />

export default Loader
