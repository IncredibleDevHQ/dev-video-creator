fragment SingleSeries on Series {
  id
  name
  organisationSlug
  picture
  description
  ownerSub
  Flick_Series {
    flicks {
      ...BaseFlick
    }
  }
}

query GetSingleSeries($id: uuid!) {
  Series_by_pk(id: $id) {
    ...SingleSeries
  }
}
