{
	"name": "prisma-orm",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"lint": "eslint ./**/*.ts* --fix",
		"seed": "ts-node seed.ts",
		"postinstall": "prisma generate"
	},
	"dependencies": {
		"@prisma/client": "^4.0.0"
	},
	"devDependencies": {
		"typescript": "^4.5.3"
	},
	"prisma": {
		"schema": "schema.prisma"
	}
}
