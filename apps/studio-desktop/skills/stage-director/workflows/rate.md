# Rate

Input: a beat, a family, geometry, roster. Run `rate_layout`. Output the four things a person can act on: score, sub-scores with the fact behind each (smallest text px, face % of chip, dwell, contrast, open side), the failed hard gate if any, and the one-change "better". Used by the layout picker, the post-take report and `/api/layout/rate`.
