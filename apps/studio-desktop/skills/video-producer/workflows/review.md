# Review

Run `frames` for: per beat start / first motion / midpoint / settle / hold end; every reframe, join, leave and seam ± 0.5 s; the first strap of each speaker. Contact sheets to `validation/review/`. Check the eight seam fields (role, readable window, outgoing, type, incoming, easing, cut/hold frame, reason). Flag hard-cut luminance jumps > 20 %, text < 18 px, a hidden speaking person, feeds out of sync. Output the three highest-gain fixes.
