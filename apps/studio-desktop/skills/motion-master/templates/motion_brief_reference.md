# motion_brief.md — exact contract

First line: `<!-- motion-master-schema: brief/v1 -->`. One `###` block per beat, in page order. Fields in this order; `(binding)` and `(Reference)` as shown. No numbers other than sentence indices and the cue word index.

```
### P03 · B02
- Move: <what the viewer must notice>                                   (binding)
- Speaker: <roster id>                                                  (binding when ≥ 2 speakers)
- Narration: sentences [4], cue word 7 ("added")                        (binding)
- Intent: relate · Hero: u-enc-mha · Supporting: u-enc-addnorm-1        (binding)
- Relationships used: link u-enc-mha → u-enc-addnorm-1                  (binding; from geometry)
- Stage: content-pip, chip S, camera: not proposed                      (Reference)
- Attribution: shared | ana | ben                                       (binding when ≥ 2 speakers)
- Composition: emphasize the source, trace the edge, pulse the target   (Reference)
- Overlays: none | lowerThird(topic) | sharedCard | …                   (Reference)
- Data class: — · Fact IDs: —
```

Rhythm check before Gate 1: no two attentional beats in a row on one hero; recap after > 3 introduced units; breathing after a camera beat. A beat that cannot state a Move is a brief defect, not a resolver problem.
