-- Each submission the product refused, as it checked it (R09 of the
-- project-flow rereview): the attempt it was, the bundle's hash, the
-- problems, and for a player check the two frames it compared and where they
-- differ. On the record itself, so a refresh, a retry and the run's next
-- attempt read the same check.
alter table studio_planning_records add column if not exists validation jsonb;
