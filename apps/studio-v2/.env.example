# Optional: keyless local system voice is used on macOS when this is absent.
FISH_AUDIO_API_KEY=

# Optional Fish Audio model override.
FISH_AUDIO_MODEL=s2.1-pro

# Optional when the browser and render worker run separately.
VITE_RENDER_WORKER_URL=
