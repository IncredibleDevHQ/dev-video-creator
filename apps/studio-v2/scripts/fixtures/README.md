# Motion repair fixtures

Python and Envoy SVG/program pairs are retained artifacts from the existing local-harness run `run-muae6b7x-80ba982d` in video notebook `video-a3a6c726-f579-4ed8-a00b-e4aaf2473ae1`. They preserve the actual authored geometry and timing that motivated the September 21 review.

`composed-review-check.mjs` keeps Python's original program and explicitly upgrades only the Envoy burst under test to scheduling version 2. These are regression fixtures, not newly generated or quality-approved videos. No narration audio is bundled.
