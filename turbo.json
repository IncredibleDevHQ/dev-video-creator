{
	"globalDependencies": [".env, **/*/.env"],
	"pipeline": {
		"prepare": {},
		"postinstall": {},
		"build": {
			"dependsOn": ["^build", "prepare"],
			"outputs": ["dist/**", ".next/**"]
		},
		"lint": {
			"outputs": []
		},
		"dev": {
			"cache": false
		}
	}
}
