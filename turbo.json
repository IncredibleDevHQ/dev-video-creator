{
	"globalDependencies": [".env, **/*/.env"],
	"pipeline": {
		"codegen": {},
		"prebuild": {},
		"build": {
			"dependsOn": ["prebuild", "^build"],
			"outputs": ["dist/**", ".next/**"]
		},
		"lint": {
			"outputs": []
		},
		"dev": {
			"cache": false
		}
	}
}
