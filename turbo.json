{
	"globalDependencies": [".env, **/*/.env"],
	"pipeline": {
		"prepare": {},
		"postinstall": {},
		"prebuild": {},
		"build": {
			"dependsOn": ["prebuild", "^build"],
			"outputs": ["dist/**", ".next/**"]
		},
		"lint": {
			"outputs": []
		},
		"dev": {
			"cache": false
		}
	}
}
